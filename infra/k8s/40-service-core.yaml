apiVersion: v1
kind: Service
metadata:
  name: core-api
  namespace: national-security
spec:
  selector:
    app: core-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: grpc
    port: 50051
    targetPort: 50051
  type: ClusterIP
