apiVersion: v1
kind: Secret
metadata:
  name: core-api-secrets
  namespace: national-security
type: Opaque
stringData:
  # In a production environment with Vault Agent Injector:
  # These would NOT be static secrets here.
  # Instead, the Deployment would have annotations:
  # vault.hashicorp.com/agent-inject: "true"
  # vault.hashicorp.com/role: "core-api"
  # vault.hashicorp.com/agent-inject-secret-config: "secret/data/national-security-platform/core-api"
  
  # For manual/GitOps (NOT RECOMMENDED for sensitive data):
  DB_PASSWORD: "placeholder_password"
  JWT_SECRET: "placeholder_secret"
